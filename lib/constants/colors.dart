import 'package:flutter/material.dart';

/// Class where all app colours are stored

class AppColors {
  static const primaryColor = Color(0xFFFFE926);
  static const secondaryDarkBlue = Color(0xFF0E1324);
  static const textColor = Color(0xFFE4ECEF);
  static const animationRedColor = Color(0xFFB60533);

  static const black = Color(0xFF010510);
}
